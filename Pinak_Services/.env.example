# Compose overrides for Pinak Services

# Prefer your in-house Parlant image built by the Build Parlant workflow
# Replace <org> and <tag> with your values. You may also digest-pin with @sha256:...
PARLANT_IMAGE_REF=ghcr.io/pinaka10/parlant:latest

# Optional: Gateway audit write location (used by tests and dev)
GOV_AUDIT_DIR=./_gov_audit
